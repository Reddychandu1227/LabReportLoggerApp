<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- CAMERA for taking picture -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Required for picking images from Gallery on Android 12 and below -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- Required for picking images from Gallery on Android 13+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />


    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_lab_report_logger"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_lab_report_logger"
        android:supportsRtl="true"
        android:theme="@style/Theme.LabReportLogger"
        tools:targetApi="31">


        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.LabReportLogger">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>